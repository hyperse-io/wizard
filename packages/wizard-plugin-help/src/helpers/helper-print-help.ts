/**
 * @description
 * Print the help message to the console.
 *
 * @param message - The help message to print.
 */
export const printHelp = (message: string) => {
  process.stdout.write(message);
};
